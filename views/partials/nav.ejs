<nav class="navbar navbar-expand-lg navbar-light bg-light">
    <a class="navbar-brand" href="/">AZrecipE</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent"
        aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <ul class="navbar-nav mr-auto">
            <li class="nav-item <%= (u) ? 'active':'' %>">
                <a class="nav-link <%= (u) ? '':'disabled' %>" href="/user">Dashboard <span class="sr-only">(current)</span></a>
            </li>
            <li class="nav-item <%= (u) ? 'active':'' %>">
                <a class="nav-link <%= (u) ? '':'disabled' %>" href="#">Preferences</a>
            </li>
            <li class="nav-item dropdown <%= (u) ? 'active':'' %>">
                <a class="nav-link dropdown-toggle <%= (u) ? '':'disabled' %>" href="#" id="navbarDropdown" role="button" data-toggle="dropdown"
                    aria-haspopup="true" aria-expanded="false">
                    Recipes
                </a>
                <div class="dropdown-menu" aria-labelledby="navbarDropdown">
                    <a class="dropdown-item" href="/user/recipes/current">Current Recipes</a>
                    <a class="dropdown-item" href="/user/recipes/previous">Previous Recipes</a>
                    <div class="dropdown-divider"></div>
                    <a class="dropdown-item" href="/user/recipes/new">Get New Recipes</a>
                </div>
            </li>
            <li class="nav-item">
                <a class="nav-link disabled" href="#">Disabled</a>
            </li>
        </ul>

        <% if (u) { %>
        <ul class="navbar-nav">
            <li class="nav-item <%= (u) ? 'active':'' %>">
                <a class="nav-link" href="#">Profile</a>
            </li>
            <li class="nav-item <%= (u) ? 'active':'' %>">
                <a class="nav-link" href="/logout">Log Out</a>
            </li>
        </ul>

        <% } else { %> 
        <a href="/auth/google" class="btn-danger btn btn-md text-uppercase">Sign in with Google</a>
        <% } %>

    </div>
</nav>